<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:ClaseNetMaui.Models"
             x:Class="ClaseNetMaui.Views.Categoria" 
             Title="Categorías">
    
    
    <VerticalStackLayout>
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <ListView x:Name="LVCat" ItemTapped="LVCat_ItemTapped" SeparatorColor="#960202">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Categoria">
                    <ViewCell>
                        <Grid ColumnDefinitions="auto, *" RowDefinitions="*" ColumnSpacing="10">
                            <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="5">
                            </VerticalStackLayout>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </VerticalStackLayout>
</ContentPage>